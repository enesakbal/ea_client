/// The code is defining an enumeration called `ClientAdapters` with two possible values: `DIO` and
/// `HTTP`. This enum can be used to represent different client adapters in a Dart program.
enum ClientAdapters {
  DIO,
  HTTP;
}
